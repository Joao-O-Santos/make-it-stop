# TODO

- [ ] Figure out ways to generate the nav bar entries automatically.

- [ ] Add configuration options in `config.mk`, for example:
	+ [ ] Add the option to also have a vertical menu/nav bar.
	+ [ ] Consider adding options for the `styles.css` (e.g., FONT).

- [ ] Add the ability to render a blog instead of a standard website.
	+ [ ] Automatically add posts (title+description) to the main
	  page.
	+ [ ] Sort blog entries chronologically (e.g., by processing
	  a <!-- PUB_DATE: year-month-date --> comment.
